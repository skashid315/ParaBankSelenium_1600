# Application Configuration
base.url=https://parabank.parasoft.com/
browser=chrome

# Timeouts (seconds)
implicit.wait=10
explicit.wait=20
page.load.timeout=30

# Static credentials for negative tests
valid.username=Shubh16
valid.password=Shubh@1636
invalid.password=WrongPass123
nonexistent.username=nonexistent123
sql.injection=' OR '1'='1
xss.script=<script>alert('xss')</script>

# ==========================================================
# SELENIUM GRID CONFIGURATION
# ==========================================================
# Set to 'true' to run tests on Selenium Grid
# Set to 'false' to run tests locally (default)
grid.enabled=false

# Selenium Grid Hub URL
# Default: http://localhost:4444/wd/hub (for local Grid)
# Example Docker Grid: http://localhost:4444/wd/hub
# Example Cloud Grid: https://hub.lambdatest.com/wd/hub
grid.url=http://localhost:4444/wd/hub
